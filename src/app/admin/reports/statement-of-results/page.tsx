import { Metadata } from 'next';
import Body from './Body';

export const metadata: Metadata = {
  title: 'Statement of Results Reports',
};

export default function StatementOfResultsReportsPage() {
  return <Body />;
}
